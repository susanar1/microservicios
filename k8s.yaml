apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservicio
spec:
  replicas: 3
  selector:
    matchLabels:
      app: microservicio
  template:
    metadata:
      labels:
        app: microservicio
    spec:
      containers:
        - name: hola-mundo
          image: susanar1/hola-mundo2:2